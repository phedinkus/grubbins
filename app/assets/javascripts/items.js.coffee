# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
$ ->
	# STYLES = "#grubbins { font-family: sans-serif; position: absolute; top: 0; left: 0; background: white; height: 100%; width: 250px; box-shadow: 4px 0px 8px darkgrey;} #grubbins input { border: none; border-bottom 1px dashed lightgrey;}"

	# styleEl = document.createElement("style")
	# styleEl.type = "text/css"
	# styleEl.innerHTML = STYLES
	# $("body").append("<div id='grubbins'><h1>the list</h1></div>")
	# $("head").append("<link rel='stylesheet' href='http://localhost:8000/assets/application.css' type='text/css' media='screen'>")
	# $("head").append(styleEl)


	# div = $("#grubbins")
	# ingredients = document.getElementsByClassName(".ingredient")
	# $.each ingredients, (ind, ingredient) ->
	# 	inputEl = document.createElement("input")
	# 	inputEl.className = "item"
	# 	inputEl.type = "text"
	# 	inputEl.value = ingredient.textContent
	# 	div.append(inputEl)